; this is a comment line
; key is outside a section
key = value

; db is a section
[db]
type = mysql
;host = localhost
; port will be integer
port = 3306
name = 'clear'
    user = clear
pass =

[api]
; on, true, yes are converted to TRUE
; off, false, no, none are converted to FALSE
; null is converted to NULL
log[enabled] = On
log[level] = debug
